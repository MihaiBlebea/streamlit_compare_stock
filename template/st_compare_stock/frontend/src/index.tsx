import React from "react"
import ReactDOM from "react-dom"
import CompareStock from "./CompareStock"

ReactDOM.render(
	<React.StrictMode>
		<CompareStock />
	</React.StrictMode>,
	document.getElementById("root")
)
